# Payment catalog refinement — 2026-09-12

Owner requested Kimi K3 UI consultation, compact bonus presentation, selection styling without card action text, aligned period selector/rail and centered payment methods. Kimi CLI k3-256k reviewed source read-only; implementation keeps existing Vue components and teal tokens. Recharge bonuses are now 0/0/5/20/75/145 for 5/49/99/199/399/599; credited totals 5/49/104/219/474/744. This supersedes CAT-05 in the earlier release record, not its historical evidence.

New subscription checkout and new reset-card purchases accept OpenAI groups only. Existing subscriptions, use-card behavior, paid-order fulfillment and historical idempotent replay remain valid. The current for-sale monthly CNY plan remains the reset price source; optional `entitlements.reset_card_purchase_price` overrides the fallback monthly/3 calculation. It must be finite, positive and at most two decimal places. The guarded catalog change sets Plus to40 and 5X Pro to180. This supersedes CAT-03's 183.33 price for future purchases. Price is revalidated under the transaction lock and stale quotes reject before debit. Admin edit preserves the override and a cleared field restores the fallback.

Period switching selects the unique same-group plan in the new period or clears selection. It cannot retain a hidden old-price selection in the order summary. Deep-linked and modal-selected plans select their matching period.

Owner confirmed customer concurrency: Plus unchanged, all5XPro terms target6; recharge5/49 unchanged,99/199/399/599 target3/4/5/6. Fulfillment only raises the user cap; any higher existing cap remains unchanged. These values configure future paid purchases and renewals, without a bulk edit of existing users.

Verification: front-end 71 focused tests, typecheck, locale completeness and ESLint pass. Root inspected actual PaymentView with local mocked data in external Chrome: light/dark desktop, card/rail alignment, centered payment icons and 375px recharge layout. Local preview blocks all API adapter traffic and performs no payment. Backend real PostgreSQL override, stale quote, no-debit non-GPT and historical replay tests pass; independent review pending. No migrations or dependency changes.

Configuration: `payment-catalog-refinement-20260912/before.json`, `intended.json`, `apply.sql`. SQL guards the exact previous catalog under a short lock and updates only the recharge setting and monthly reset prices and5XPro concurrency metadata. Apply only after both runtime nodes deploy the override-aware build. No group rate, wallet, historical order, grant, update is included. New purchase snapshots carry the configured concurrency benefit.

Release and rollback: use deploy/README.md build_only workflow and canonical lock-owning receivers, candidate background active, old origin preserve-standby. Retain the prior34d2b8d4 image. On rollback, disable new reset-card checkout before returning to a binary that lacks override support (otherwise it would charge550/3); preserve successful purchases and balances. Runtime evidence will be appended after execution.
