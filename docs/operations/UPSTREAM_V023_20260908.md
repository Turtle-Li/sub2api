# Upstream v0.2.3 compatibility and release

Task SUB2-UPSTREAM-20260908: owner requested upstream merge, release and a
read-only infrastructure traffic/cost audit. Baseline fork main is
`2a9b18e5a6fc5c80b0264e6404a4ebe7550e0dd4`; upstream v0.2.3 is
`8fa67d477d6651a744754392a8982ea589c26ae6` at
https://github.com/Wei-Shaw/sub2api. Preserve existing LGPL-3.0 notices.

Acceptance: incorporate the released fixes, retain existing local gateway and
billing behavior, preserve group policies and old/new database access, verify
source and artifact before updating both serving Azure and standby old origin.
Do not transfer background ownership or change DNS. Shared provider capacity
failures do not establish a new-origin regression or justify DNS rollback.

Delivery DAG: T1 root pins upstream and live node state; T2 migration owner
adapts migration 236 and actual PostgreSQL regressions while root integrates
and tests the shared request paths; T3 independent QA/review checks the frozen
source and evidence; T4 root publishes the tested main, explicitly dispatches
a build-only GitHub image, verifies its hash/labels, backs up the shared data,
and releases through installed maintenance-lock-owning image receivers. T5
verifies both node roles, request gates, public health and schema/policy parity.
The independent infrastructure audit has no resource mutation ownership.
A failed source/artifact/migration gate blocks release. Existing blue-green
health/traffic rollback gates remain unchanged; database recovery uses the
project backup procedure, never a speculative reverse migration.

## Upstream contract and intentional storage difference

Inspected fixed upstream migration 236 and its repository integration tests,
account test model display helpers, Ollama auth/token-clamping helpers and
Messages/Responses/Chat Completions call sites and tests. Ollama behavior is
scoped by actual destination/provider; no new production feature is enabled.

The fork's established contract is documented in
`UPSTREAM_V022_ALLOWLIST_20260907.md`: Ent `model_allowlist` maps to physical
`models_list_config`. Upstream migration 236 instead renames this column,
which would break both current Ent and draining/rollback generations.
Keep migration 235 unchanged (its checksum is already deployed). Adapt 236
to converge on the fork's physical canonical column. Existing canonical JSON,
including `{}` and disabled policies, wins when both columns exist; stale
alternate policies must not enable restrictions. When only the upstream
column exists it can be renamed back; when neither exists add canonical
storage. Retain an alternate column if already present, without claiming it
is synchronized. Production is expected to have only canonical storage.

Validate legacy-only, upstream-only, both, missing, repeated and non-public
schema cases using real PostgreSQL, plus rolling Ent/legacy SQL integration.
A future physical rename requires a separate migration decision after all
old readers and rollback images have been retired. Root owns integration and
final release; no provider, account, group policy or DNS mutation is included.
