# Upstream v0.2.4 merge and production release

Owner request: pull, merge and publish the new upstream release. Baseline fork
main is `cfda70bcbab320820023079ce3123f688d6235bb`. Reference repository is
https://github.com/Wei-Shaw/sub2api, annotated tag `v0.2.4`
(`d681d0798064ee0ffff376d19687d12f09fe600f`). The release tag still contains
VERSION 0.2.3; use 0.2.4 as corrected by upstream main
`98d86915becae9fe9491a91ffc6defd5235c8d2b`. Preserve LGPL-3.0 notices.

Acceptance: incorporate released upstream fixes while retaining local fixed
OAuth egress, native image admission and multi-image behavior, payment pilot
boundaries, canonical group storage, and rolling rollback compatibility.
Azure `sub2api-candidate` remains accepting/background active; old origin
`sub2api-new` remains accepting/background standby. No DNS, provider policy,
payment enablement or cross-node background ownership change is included.

## Integration seams and contracts

- Proxy management: preserve local transaction locks, account version/CAS,
  retired-account fencing and probe invalidation while incorporating upstream
  partial updates and directed/repeated expiry fallback. Reference sources:
  `backend/internal/handler/admin/proxy_handler.go`,
  `backend/internal/repository/proxy_repo.go`,
  `backend/internal/service/admin_proxy.go`, and their regression tests.
- Images: adopt upstream Image 2.5 models and configurable Responses driver
  (`SUB2API_IMAGES_MAIN_MODEL`, default `gpt-5.6-luna`); retain local MaxN,
  verbatim prompt and exactly-N parallel image instructions. Reference sources:
  `openai_images.go`, `openai_images_responses.go`, `openai_codex_transform.go`
  and `openai_images_model_test.go` under backend service. Combined regression
  checks Image 2.5 + driver override + multiple outputs. Native HTTP/WS image
  admission remains governed by RESPONSES_IMAGE_ACCOUNT_ROUTING_20260907.md.
- Migration `237_add_minimax_platform.sql` widens platform constraints only.
  It does not modify existing rows or group model storage. The existing
  `237_unified_payment_refund_attempts.sql` remains unchanged: the migration
  runner keys/checksums by full filename, so the shared number is not identity.
  Preserve deployed migration 235/236 bytes and `models_list_config` storage.
- MiniMax and Grok media capabilities enter the normal upstream gates; no
  existing account is reconfigured or new feature enabled as part of release.

## Gates and rollback

Independent proxy/account merge owners, root image integration and verification,
then independent source review/QA. Validate affected frontend behavior and build,
backend unit tests and real PostgreSQL migrations/rolling-storage integration.
Publish exact tested main, build-only on GitHub Actions, verify compressed image
metadata/digest, back up shared data, and use installed image receivers with
maintenance locking and real-request/health rollback gates. Release Azure with
activate and the old origin with preserve-standby; keep prior images available.
Financial tables are never removed to roll back an application image.
